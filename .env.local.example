# Variáveis de ambiente — copie para .env.local e preencha com seus valores

# Banco de dados Vercel Postgres
DATABASE_URL=

# JWT — gere um segredo forte: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
